// Client config feature: edit things that live outside the asset files of the
// Tibia client — the `conf/config.ini` URLs and the client-side BattleEye gate
// in `client.exe`. Used for private-server / OT work on a client the user owns.

pub mod commands;
pub mod parsers;
